import { CloseOnDirective } from './close-on.directive';

describe('CloseOnDirective', () => {
  it('should create an instance', () => {
    // const directive = new CloseOnDirective();
    // expect(directive).toBeTruthy();
  });
});
