import { CheckElemCoordsDirective } from './check-elem-coords.directive';

describe('CheckElemCoordsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckElemCoordsDirective();
    expect(directive).toBeTruthy();
  });
});
